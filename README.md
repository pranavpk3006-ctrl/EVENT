This app named FACILIFY is an event management app which has currently 4 section which can be accessed from the bottom section of the app.
There are 4 section namely  Home , Facility , Team  and Events .
Currently we should work on single inrterface instead of both admin and user. We could further make single interface in which some part are accessible (editable) to admin and rest are just in read only opde for the user .

